# Handoff 2026-09-16e — audyt PR #38 (gałąź `arena/01a0ab16-okolica`, PR #39)

## Gdzie jesteśmy

Sesja bez uwag z terenu (zlecenie „kontynuujemy projekt”) wykonała to, co każe
`AGENTS.md` §2 pkt 2: **audyt poprzedniego scalonego PR (#38, squash `df15f70`
na `31dbbf4` — 34 pliki, +707/−229)**. Kamienie M0–M12 pozostają zamknięte jako zakres
kodu (LESSONS L68); kolejka pracy to uwagi z testów terenowych (BACKLOG bez pozycji
do samodzielnej promocji, brak issues na GitHubie).

Brama startowa i bieżąca: `npm test` **829 / 829**, budżet **99 928 / 100 000**
(rezerwa 72), cache w `main` = `m12-152`.

## Wynik audytu PR #38 (fala A na kroku 5 + B26)

**Zgodne, bez zastrzeżeń:**
1. **Wersje spójne** — `?v=m12-152` we wszystkich importach i `index.html`,
   `WERSJA_SW = m12-152`, zero resztek `m12-149` w kodzie.
2. **A(a)** — akapit instrukcji usunięty; pin kontraktu pilnuje PUSTKI między
   `</h2>` a `<textarea>`; uczciwość o jawnej paczce niosą karta prywatności
   i README (piny przepisane na nośniki wykonujące obietnicę).
3. **A(b)** — przycisk i nasłuch schowka usunięte, zero odwołań do
   `przycisk-wklej` w żywych nośnikach; nasłuch `paste` zostaje; ADR 0006 →
   aneks 2026-09-16d; `WORKFLOW`/`ARCHITECTURE` przepisane; dwie martwe frazy
   w strażniku dryfu.
4. **A(c)** — `wyczyscEkranPaczki()` przy WEJŚCIU na krok 5 (z `status('')`)
   i w `wrocNaPoczatek()`; oba wejścia na krok 5 to nawigacja, więc czyszczenie
   niczego nie gubi; `dataset.stan` spójny; test dwóch gier z odrzuconą paczką;
   L78 w rejestrze i archiwum.
5. **B26** — `app/styles.css` bez zmian (brak w diffie); aneksy ADR 0011/0042
   z 2026-09-16d; notki w rejestrze; B26 rozstrzygnięte w BACKLOG; pin kontraktu
   na obie strony decyzji.
6. **Archiwizacja budżetowa** — treść przeniesiona, nie skasowana (trzecia tura
   ADR 0006 w archiwum, nowe pliki 0006-15/15d, 0011-12/14, 0042-15); żywe pliki
   niosą wskaźniki; pin m12-115 przepisany na archiwum.
7. **Brama z handoffu 16d powtórzona w całości i zgodna** (testy, check, WCAG,
   zasięg mostu, budżet — tabela niżej); CI na PR #38 zielone (`test` SUCCESS).

**Znalezione defekty (dwa, redakcyjne, poprawione w miejscu w commicie `49d3f76`):**
1. Rejestr ADR, wiersz 0042: zdublowane słowo („przyciski-stopki stopki”)
   i niezbalansowany nawias (2× `(` vs 3× `)`) — wiersz przepisany.
2. `HANDOFF_2026-09-16d`, tabela „Brama jakości”: budżet 99 963 (rezerwa 37)
   zamiast końcowego 99 928 (rezerwa 72) — komórka poprawiona (opis PR #38,
   sekcja 16d w `PROJECT_HISTORY` i pomiar na `df15f70` mówią 99 928).

## Uwagi terenowe właściciela (ta sama sesja, po audycie) — mismatch, tryb ręczny, Overpass, cache L2

Cztery uwagi z telefonu, zrealizowane krokami A–E (commity `ce2c98c`,
`b922200`, `9bbf195`, `0bc6731`, `56e705f`; cache `m12-153`):
1. **Mismatch paczek: jedna linijka** (A) — karta nie wymienia powodów;
   `powodyNiedopasowania` filtruje jak dotąd, UI go nie cytuje; ADR 0046 →
   aneks 2026-09-16.
2. **Tryb ręczny stacji usunięty w całości** (B) — drag pinezek nie działał
   na iPhonie. Zniknęły przycisk, `ustawTrybReczny`, gałęzie, wzmianki
   (S02/S03/S09/S13/S14) i style; degradacja to cache albo jawny pierścień;
   ADR 0005 → aneks 2026-09-16.
3. **Overpass: dokładny klucz + skan pokrycia** (C) — wpis niesie kotwicę
   pobrania (`srodek`, `promienM`, `tryb`); po pudle dokładnym odczyt szuka
   w tej samej komórce wpisu pokrywającego (`wybierzWpisSieci`, najświeższy);
   ADR 0010 → aneks 2026-09-16.
4. **Wspólny cache sieci na Drive, L2** (D) — katalog `okolica-sieci-cache`,
   akcje `akcja=siec` / `siec-zapisz`, odczyt L1→L2→Overpass, trafienie L2
   dokarmia L1, zapis w tle po świeżym pobraniu; nowy ADR 0052; **most Apps
   Script wymaga ponownego wdrożenia** (procedura w
   `docs/setup/most-drive-instrukcja.md`) — bez tego L2 śpi, a aplikacja po
   cichu jedzie do Overpass.
5. **Krok E** — wersje `m12-153`; budżet lektury 100898 → **99998 / 100000**
   (rezerwa 2 — archiwizacja 4 aneksów 0016 i m12-94 z 0036 plus docięte
   własne teksty; następna sesja MUSI archiwizować, zanim cokolwiek dopisze).

Live-verify: `?v=m12-153` i `WERSJA_SW` serwowane, brak 404 na modułach.

## Brama jakości

| Sprawdzenie | Wynik |
| --- | --- |
| `npm test` | **843 / 843** |
| `npm run check` | OK — szablon §2 (3 603 znaki), §2.2 (3 738) |
| `npm run audyt` (WCAG) | 0 naruszeń |
| `npm run zasieg-mostu` | 97,9% (936/956) |
| `npm run budzet` | **99 998 / 100 000** (rezerwa 2) |
| wersje | protokół `PYT/1.1`, szablony `PYT/1.1.2` / `PYT/1.1-nofc.2`, cache `m12-153` |
| CI (GitHub Actions, PR #38) | zielone — `test` SUCCESS |
| CI (GitHub Actions, PR #39) | zielone — `test` SUCCESS |

## Co właściciel ma zrobić

1. **Scalić PR tej sesji (#39)** (squash).
2. **Wdrożyć most Apps Script na nowo** (wkleić `docs/setup/apps-script-repo-paczek.gs`,
   Wdróż → nowa wersja; standardowa procedura z `most-drive-instrukcja.md`) —
   inaczej cache L2 nie zadziała (aplikacja po cichu jedzie do Overpass).
3. **Kolejne testy w terenie** — aplikacja czeka na nowe uwagi i obserwacje
   z testów na telefonie (iPhone); w kolejce bez zmian: paczka z promptu
   `PYT/1.1.2` (zasada 7, równa liczba pytań na stację, kotwiczenie) oraz
   polecenia z PR #35.

## Otwarte

- Kolejka pracy: **pusta** — kamienie M0–M12 zamknięte, czekamy na uwagi
  z terenu (LESSONS L68).
