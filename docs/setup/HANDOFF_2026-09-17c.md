# Handoff 2026-09-17c — kolejka terenowa A/B/C1/C2/D (uwagi właściciela z iPhone)

Kontynuacja sesji 2026-09-17b na tej samej gałęzi (`arena/01a0aed7-okolica`)
i w tym samym PR-ze: **PR #41**. Wpisane uwagi z testu terenowego właściciela
(2026-09-17, telefon) — pięć punktów, wszystkie zrealizowane.

## Co weszło i jak jest pokryte

| Uwaga | Zmiana | Commit | Pokrycie |
| --- | --- | --- | --- |
| A | pinezka zaliczonej stacji POMARAŃCZOWA i pusta w środku (szary wypadł — zlewał się z zielonym) | `f3ca16a` | `test/mapa.test.js`, pin w `test/kontrakt.test.js` (brak `#6b7280`) |
| B | wejście na „Stacje w Twojej okolicy” startuje CZYSTE: zero danych poprzedniej gry | `8e890a6` | `test/aplikacja.test.js` („uwaga B (2026-09-17)”, dwa setupy pod rząd, drugi przy wiszącym Overpassie) |
| C2 | pulsujące „Łączę z siecią…” NATYCHMIAST po przyjęciu wklejki, gaśnie po odpowiedzi mostu | `614e3a6` | `test/zestawy-ui.test.js` („uwaga C2”, most na bramce) |
| C1 | cztery okrągłe ikony modeli nad wklejką; wybór opcjonalny; `model` w `meta` paczki i znaczek przy propozycji | `e716f03` | `test/zestawy.test.js` (addytywność), `test/zestawy-ui.test.js` (ikony, przełączanie, POST, znaczek), pin ADR 0053 w `test/kontrakt.test.js` |
| D | po starcie w lobby ZERO ekranów przejściowych: od razu odcinek, mapa + mini-pasek | `83666df` | `test/wieloosobowa-ui.test.js` (host i gość, przed i po odliczaniu), pin 5b w `test/kontrakt.test.js`, aneks ADR 0044 |

Dokumentacja: aneks B w ADR 0005, aneks C2 w ADR 0011, **nowy ADR 0053**
(model AI) + wiersz w `docs/decisions/README.md`, aneks D w ADR 0044.
Lekcja **L80** (atrapa DOM jest globalna — kolejność testów w pliku ma znaczenie)
w `docs/LESSONS.md` + `docs/LESSONS_ARCHIVE.md`.

## Brama jakości (stan po ostatniej zmianie dokumentów)

| Sprawdzenie | Wynik |
| --- | --- |
| `npm run brama` | **EXIT 0** |
| `npm test` | **852 / 852** |
| `npm run check` | OK — szablon §2 (3 603 znaki), §2.2 (3 738) |
| `npm run audyt` (WCAG) | 0 naruszeń (jasny i ciemny motyw) |
| `npm run budzet` | **99 431 / 100 000** (rezerwa **569**) |
| wersje | protokół `PYT/1.1`, szablony `PYT/1.1.2` / `PYT/1.1-nofc.2`, cache **`m12-158`** |
| serwer podglądu | `npm run serwer` na `0.0.0.0:8000` |

Kontrole nowych testów (bez zmiany kodu test musiał czerwienieć): wyłączony
`znaczekModelu`/`renderujModeleAi` (C1), wyłączone `przygotujEkranStacji()`
(B), wyłączone `wklejkaCzekanie` (C2) — wszystkie trzy kontrole potwierdzone.

## OTWARTE — blokada poza kodem

1. **GitHub: token wygasł.** `git push` i `gh` zwracają
   `could not read Username for 'https://github.com'` /
   `The github.com token in GH_TOKEN is no longer valid`.
   Wypchnięte wcześniej w tej sesji: `8e890a6` (B) i `614e3a6` (C2).
   **Lokalnie gotowe do wypchnięcia: `e716f03` (C1) + commit z handoffem,
   historią i L80.** Po ponownym połączeniu GitHuba w Arenie wystarczy
   `git push origin arena/01a0aed7-okolica`, potem aktualizacja opisu PR #41
   (`/home/user/pr-body.md`) i sprawdzenie CI.
2. **CI na PR #41** — nie sprawdzone dla `f3ca16a`, `83666df`, `8e890a6`,
   `614e3a6` (brak dostępu do API w chwili pisania tego handoffu).
3. **Właściciel: ponowne wdrożenie mostu Apps Script** (cache sieci L2,
   ADR 0052 + lustrzana `TOLERANCJA_KOTWICY_M=200`) — procedura
   `docs/setup/most-drive-instrukcja.md` §184-190. Bez tego wspólny cache L2
   po cichu nie odpowiada.
4. **Test terenowy na iPhonie** (świeży `?v=m12-158`): A — pineski zaliczonych
   stacji pomarańczowe i puste w środku; B — „Stacje w Twojej okolicy”
   w nowym setupie startuje bez śladu poprzedniej gry; C2 — pulsujące
   „Łączę z siecią…” zaraz po wklejeniu; C1 — cztery ikony nad wklejką,
   wybrany model widoczny przy propozycji paczki; **D — po kliknięciu startu
   w lobby od razu mapa + mini-pasek, bez żadnego ekranu przejściowego**.
5. **Rezerwa budżetu 569 tokenów** — następny aneks terenowy ma jeszcze miejsce,
   ale każda kolejna treść w lekturze obowiązkowej wymaga pomiaru
   (`npm run budzet`) przed commitem; przy przekroczeniu obowiązuje kolejność
   z L79 (najpierw archiwizacja aneksów, potem nowa treść).

## Wskazówki dla następnej sesji

- Sprawdź najpierw, czy `e716f03` (i commit dokumentów) są już na `origin`;
  jeśli nie — wypchnij je jako pierwsze (gałąź sesji, bez force push).
- Uwaga D była zgłaszana przez właściciela kilka razy — przy wątpliwościach
  nie „poprawiaj” jej po raz kolejny kodem: przeczytaj pin 5b w
  `test/kontrakt.test.js` i test „uwaga D” w `test/wieloosobowa-ui.test.js`.
- Nowe testy w `test/zestawy-ui.test.js` dopisuj z głową: patrz L80 (kolejność
  testów w pliku ma znaczenie, bo atrapa DOM jest globalna).
