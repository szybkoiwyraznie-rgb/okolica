# Handoff 2026-09-15d — paczka AI nie lądowała na Drive (D2), fokus pola na iOS, puste pole po błędzie, ekran stacji bez spisu (gałąź `arena/01a0a569-okolica`, PR #32, m12-136)

## Gdzie jesteśmy

Cztery uwagi terenowe właściciela z 2026-09-15 zaimplementowane i zweryfikowane,
w tym jedna KRYTYCZNA: wklejona paczka pytań AI nie tworzyła pliku na Drive.
Brama zielona (**827 testów**, `npm run brama`), budżet lektury pod progiem
(**99 817 / 100 000**, rezerwa 183 — mało, patrz „Otwarte"), aplikacja
obejrzana w headless Chromium na 360 px i 1334×750. Liczby są stanem KOŃCOWYM:
w połowie sesji było 829 testów i rezerwa 589, ale usunięcie martwego przycisku
zabrało dwa testy funkcji `poprawkaDlaModelu`, a wpisy w dokumentach zjadły
resztę rezerwy.

## Co zrobiła ta sesja

1. **(6) Paczka AI nie lądowała na Drive — usterka D2, KRYTYCZNA.**
   `przyjmijKandydata` w `docs/setup/apps-script-repo-paczek.gs` szukał pliku
   przez `getFilesByName(nazwa).next()` BEZ `hasNext()`. W Apps Script
   `FileIterator.next()` na pustej kolekcji **rzuca wyjątek** (dokumentacja
   Drive), więc dla każdej NOWEJ nazwy — czyli zwykłego przypadku — `doPost`
   łapał wyjątek i odpowiadał `{ ok:false, blad }`. Aplikacja pokazywała
   „Paczka przyjęta, ale Drive odrzucił wysyłkę", gra toczyła się dalej, a pliku
   nie było. Wprowadził to PR #30 (`fa8d9d8`, ADR 0048); PR #31 mostu nie
   dotykał. Naprawa: `pierwszyPlikNazwa(katalog, nazwa)` = `hasNext() ? next()
   : null` w obu wyszukiwaniach i w sprawdzeniu katalogu odrzuconych.
   Most wymagał ponownego wdrożenia na Apps Script (plik w repozytorium jest
   naprawiony, ale po stronie Drive wykonuje się wdrożona kopia) —
   **właściciel wdrożył**, patrz „Domknięte po uwagach właściciela".
2. **Atrapa Drive rzuca tak jak platforma (L73).** `test/helpers/most.js`:
   `next()` po wyczerpaniu kolekcji rzuca `No next element`, a
   `iteratorAtrapyDrive` jest eksportowany, żeby test mógł to przypiąć. Wcześniej
   atrapa oddawała `undefined`, więc 826 testów było zielonych przy zepsutym
   kontrakcie — to dlatego dwa poprzednie audyty uznały most za czysty.
3. **(4) Pole wklejenia jest puste po błędnej paczce.** Nasłuch `paste` woła
   `e.preventDefault()`: przeglądarka wstawia tekst PO powrocie z nasłuchu, więc
   czyszczenie pola w `pokazOdrzuconaPaczkeAi()` było natychmiast nadpisywane.
   Atrapa `wklej()` w `test/helpers/dom.js` odtwarza teraz tę kolejność (nasłuchy,
   potem domyślna akcja, chyba że zablokowana) — bez `preventDefault` w kodzie
   5 testów jest czerwonych.
4. **(3) Strona nie przybliża się na fokusu pola.** iOS Safari przybliża stronę,
   gdy pole z fokusem ma `font-size` < 16 px, a `.pole-tekstowe` miało 14 px —
   i nie dało się oddalić (pinch poza mapą zablokowany celowo, ADR 0047; pinch
   na mapie rusza kafelkami). Teraz 16 px, a kontrakt CSS pilnuje, żeby żadna
   reguła dotykająca `input`/`textarea`/`select`/`.pole-tekstowe` nie schodziła
   poniżej 16 px. Właściciel miał rację: winny był fokus, nie gest.
5. **(5) Ekran stacji bez spisu.** `#lista-stacji` (numer, opis, współrzędne,
   dystans) zniknął z HTML, CSS i `renderujStacje()`; jest `#stacje-podsumowanie`
   z jednym zdaniem: z siecią dróg „Wygenerowano i zlokalizowano N stacji.", przy
   pierścieniu „Wygenerowano N stacji." (bez słowa „zlokalizowano", bo byłoby
   nieprawdą — L6; powód mówi niezmienione `#stacje-tryb`). Tryb tajnej trasy
   (ADR 0034) został przy swoim zdaniu — właściciel wyłączył go z tej zmiany.
6. **Budżet lektury.** Sesja zaczęła się od 100 189 tok (przekroczenie), a po
   wpisach z sesji było 101 275. Do `docs/decisions/archive/` wyszły aneksy:
   ADR 0019 z 2026-09-13c, oba ADR 0024 z 2026-09-07 (drugi odwrócony przez
   ADR 0046) i ADR 0005 z 2026-09-14 (m12-120; reguła zostaje w LESSONS L71).
   Stan po tym kroku: 99 411 / 100 000; po wszystkich wpisach sesji
   (aneksy ADR, L73, B23) — 99 692 / 100 000.

## Audyt PR #31 — werdykt (uzupełniony)

Treść PR #31 bez defektów (ADR 0049, szablony `PYT/1.0.10` / `PYT/1.0-nofc.5`,
jeden komunikat odrzucenia paczki, m12-132). **Ale audyt PR #30 z poprzedniej
sesji („2026-09-15c": „no defects") był niepełny** — przepuścił opisany wyżej
brak `hasNext()`. Wniosek zapisany w L73: przegląd diff-a, który nie konfrontuje
wywołań obcego API z jego udokumentowaną semantyką, przepuszcza całą tę klasę
usterek.

## Weryfikacja (co dokładnie uruchomiono)

- `npm run brama` → **827/827**, `npm run check` (oba szablony §2/§2.2:
  3894 / 4142 znaków), `npm run audyt` (WCAG AA: 0 naruszeń),
  `npm run budzet` (99 817 tok).
- Zęby testów mostu: z przywróconym ciałem `przyjmijKandydata` z PR #30 →
  10 z 18 w `test/most-indeks.test.js` czerwonych; z naprawą 18/18.
- Zęby testów wklejenia: bez `e.preventDefault()` → 5 z 129 w
  `test/aplikacja.test.js` czerwonych.
- `npm run zasieg-mostu` → 883/903 linii (97,8%), 12 profili.
- Headless Chromium (`/home/user/.narzedzia`, przepis ENVIRONMENT §4.1),
  360×740 i 1334×750, `?tryb=test`, pozycja z mapy: ekran stacji widoczny,
  `#lista-stacji` nie istnieje, `#stacje-podsumowanie` = „Wygenerowano 5 stacji."
  (pierścień — egress zablokowany, więc bez Overpass), 5 pinezek, brak
  poziomego przewijania, 0 błędów konsoli; `#pole-odpowiedz` i `#pole-prompt`
  mają wyliczone 16 px. Wklejenie śmiecia w pole paczki: `defaultPrevented=true`,
  pole puste, stały komunikat błędu.

## Domknięte po uwagach właściciela (2026-09-15, druga tura)

- **Most wdrożony na Apps Script** — właściciel potwierdził; zapis paczek do
  katalogu zaakceptowanych działa. Z sandboxa nie da się tego sprawdzić
  (egress zablokowany, L3), więc potwierdzeniem jest komunikat właściciela.
- **`przycisk-poprawka` usunięty** (commit `8048302`, m12-135) razem z
  `poprawkaDlaModelu`, `STAN.poprawkaFactcheck` i obietnicą w komunikatach E02;
  nośniki przepisane, pin odwrócony, trzy martwe frazy w `dryf-dokumentow`,
  ADR 0006 aneks. Brama: 827/827 (dwa testy funkcji odeszły razem z nią).
- **Pusta lista usterek `#wynik-usterki` usunięta** (commit `308f550`, m12-136,
  BACKLOG B23 — właściciel: „tak, usuń"). Lista nie miała dzieci nigdy:
  `renderujUsterki()` miała dwa wywołania i oba z pustą tablicą, a `#wynik-usterki`
  był czyszczony także na starcie walidacji. Zniknęły `<ul>`, funkcja,
  `listaUsterek` i reguły `.usterki`; karta `#wynik-walidacji` z nagłówkiem
  i `data-stan='blad'` została. Pin przepisany na sprawdzanie HTML — atrapa DOM
  tworzy brakujący element na żądanie, więc asercja na stubie nic by nie
  pilnowała. Wskazówka w LESSONS L54 kieruje teraz do `walidujPaczke()`, dwie
  martwe frazy w `dryf-dokumentow`, dwa aneksy ADR 0006 z tego dnia scalone
  w jeden (ta sama decyzja). Brama: 827/827, budżet 99 817/100 000.

## Otwarte

1. **Budżet lektury ma 183 tokeny rezerwy** — pierwszy większy aneks w
   następnej sesji przekroczy próg i skracanie stanie się obowiązkowe
   (AGENTS.md §0). Kandydaci do archiwum: aneksy ADR 0043/0044 z 2026-09-13b.
2. **Weryfikacja właściciela na iPhonie**: brak przybliżenia HTML przy fokusu
   pola odpowiedzi, puste pole po błędnej paczce, ekran stacji z jednym zdaniem,
   plik paczki w katalogu zaakceptowanych na Drive.
3. **ADR 0047 czeka na potwierdzenie z iPhone'a** (blokada pinch poza mapą) —
   z poprzedniej sesji, dalej niepotwierdzone.
