# Handoff 2026-09-15g — audyt PR #34, naprawa dryfu „rozkład równy ±1" (gałąź `arena/01a0a6d9-okolica`, PR #35, m12-144)

## Gdzie jesteśmy

Sesja zaczęła się od „kontynuujemy projekt" bez uwag z terenu, więc domknęła
audyt PR #34 (AGENTS.md §2 pkt 2). Audyt znalazł jeden dryf dokumentacji po
decyzji B25 (E05 bez tolerancji ±1): trzy miejsca dalej opisywały starą regułę.
Naprawione w tym samym PR. Brama zielona: **807 testów**, szablony zgodne
z dokumentem, audyt WCAG **0 naruszeń**, zasięg mostu **97,7%** (850/870),
budżet lektury **99 639 / 100 000** (rezerwa **361**), cache **m12-144**,
szablony **`PYT/1.1.2` / `PYT/1.1-nofc.2`**, protokół **`PYT/1.1`**.
**Kolejka pracy pusta** — kamienie M0–M12 zamknięte, BACKLOG bez nowych tematów.

## Co zrobiła ta sesja

1. **Audyt PR #34 (c1a90e8, 22 pliki, +515/−62).** Spójne: `E05` bez tolerancji
   ±1 (B25), werdykt z `wpis.poprawna` (ADR 0050 aneks), trzy zdania promptu
   w obu szablonach + łatki `PYT/1.1.2`/`PYT/1.1-nofc.2` i wpisy §7, nowe testy,
   reszta `app/*.js` to tylko `?v=m12-140 → m12-143`.
2. **Znaleziona usterka (dryf dokumentacji):** PROTOKOL §3.2 (wiersz `stacja`),
   komentarz `app/rozgrywka.js` (`stacjaZamknieta`) i ADR 0015 (Kontekst) nadal
   mówiły „rozkład równy ±1", choć `E05` już tej tolerancji nie ma. Strażnik
   dryfu nie miał pinu na tę frazę (a aktywne ADR-y są poza listą nośników).
3. **Naprawa:** §3.2 opisuje stan faktyczny (każda stacja ≥ 1 pytanie; rozkładu
   nie sprawdzamy od 2026-09-15f, liczba pytań wynika z setupu, patrz §6 `E05`);
   komentarz i ADR 0015 przepisane bez „±1"; pin frazy w `MARTWE_FRAZY`
   (`test/dryf-dokumentow.test.js`) + LESSONS **L76**; dopisek do B25 w BACKLOG.
   Archiwalny aneks `aneksy-0024-2026-09-07b.md` bez zmian (historia).
4. **Cache-bust `m12-143 → m12-144`** (zmiana `app/*.js` — L29/L37).

## Co właściciel ma zrobić

1. **Obejrzeć i scalić PR #35** (squash, jak dotąd). Kolejka pracy jest pusta —
   bez uwag z terenu nic się nie dzieje.
2. **W terenie (jak po 15f):** sprawdzić paczkę z promptu `PYT/1.1.2` — czy
   pytania nie zawierają odpowiedzi (zasada 7), czy model trzyma równą liczbę
   pytań na stację (walidator tego nie pilnuje) i czy kotwiczenie bez przepisu
   o nazwie miejsca dalej daje pytania zakotwiczone.

## Otwarte

- PR #35 czeka na scalenie.
- Kolejka pracy pusta — tryb „audyt i czekaj" do kolejnych uwag z terenu.
