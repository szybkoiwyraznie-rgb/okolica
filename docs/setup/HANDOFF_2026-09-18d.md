# Handoff 2026-09-18 (d) — sesja `arena/01a0b4de-okolica`, PR #46

Stan gałęzi: `arena/01a0b4de-okolica` (PR #46, draft, baza `main`).
Brama: `npm test` 862/862, `npm run brama` EXIT 0, budżet lektury
99 583 / 100 000. Wersja cache `?v=`/`WERSJA_SW`: `m12-165`.

## Co się wydarzyło w tej sesji

1. **Audyt PR #45** (obowiązkowy przed kodowaniem): bez zastrzeżeń —
   bramka ADR 0061 spójna z walidacją R08 i `nowaRozgrywka`, etykieta
   „🧒 Dziecko” na obu rendererach, cache-bust jednolity. Wpis w historii.
2. **Uwaga terenowa 1 (Ekran Stacje)** — wdrożona decyzja właściciela:
   czerwona karta błędów sieci pod jawnym stopem zniknęła całkowicie.
   - `pobierzSiec`/`grafDlaTrybu`/catch `przeliczZTegoCoJest` nie tworzą
     kart (S03/S09); `zablokujStacje()` i `pierścieńTrybTestowy()`
     czyszczą `bledy-stacje` — stan końcowy ekranu to jedna pełna
     wiadomość;
   - karty przy istniejących stacjach (S12/S14, `zlozKarteUsterekStacji`)
     zostają — gra toczy się dalej, nie dublują stopu;
   - diagnostyka prób: wyłącznie ⓘ Informacje (`#siec-proby` + status);
   - WE03 nie obiecuje już „trybu uproszczonego” (wskazuje: ponowienie
     sieci, zmiana okolicy lub trybu);
   - ADR 0061: aneks 2026-09-18 (pkt 5 o karcie S09 nie obowiązuje);
   - testy: nowy scenariusz właściciela (gra realna, Overpass odmawia →
     stop bez karty) + odwrócone piny + kontrakt + dryf;
   - **weryfikacja na żywo** headless Chromium (gra realna, wstrzyknięty
     GPS, brak internetu): jawny stop z „↻ Pobierz sieć ponownie”,
     `bledy-stacje` puste, próby w Informacjach.

## Zadania właściciela (narastająco, bez zmian poza uzupełnieniem)

- **Wdrożyć `.gs` ze schematem `sieci/2`** na most Drive (stare wdrożenie
  nie zna nowego schematu cache).
- **Skasować 3 stare pliki L2** na Drive (zadanie z handoffu 2026-09-18b).
- **Test iPhona** — teraz na `m12-165`; przy okazji sprawdzić w terenie,
  czy jawny stop BEZ czerwonej karty czyta się dobrze (uwaga terenowa 1)
  i czy bramka „bez sieci gra nie startuje” (ADR 0061) nie irytuje
  bardziej niż pierścień (odwrotka jednym aneksem, kod w jednym miejscu).
- Etykieta multi „🧒 Dziecko” bez wieku — potwierdzenie z terenu wciąż
  otwarte.

## Uwagi dla następnej sesji

- Pętla startowa: AGENTS §0 całość; §2 najpierw audyt scalonego PR, potem
  kod; bez backlogu czekamy na uwagi z terenu (L68).
- Budżet lektury: rezerwa 417 tok — nowe aneksy ADR najpierw archiwizować
  albo skracać (L62/L79); mierzyć `npm run budzet` po ostatniej zmianie.
- Weryfikacja live bez egressu: headless Chromium z `/home/user/.narzedzia`
  (przepis ENVIRONMENT §4.1; po resecie sandboxa instalacja od zera —
  kilkanaście sekund). Kluczowe triki: `#przycisk-start-zacznij` otwiera
  setup; localStorage `okolica:gracze` = zapamiętany gracz (jak
  `test/helpers/dom.js`); GPS przez `evaluateOnNewDocument`.
