# Handoff 2026-09-18b — audyt PR #44 + bramki startu gry multi (dopełnienie ADR 0061)

Gałąź sesji: `arena/01a0b45e-okolica`, PR **#45** (draft, baza: `main` = `c54e018`).
Kontynuacja handoffu 2026-09-18 (wieczór) — bez nowych uwag z terenu; pętla
sesji to obowiązkowy audyt poprzedniego PR + domknięcie bramki ADR 0061 na
wejściu do gry sieciowej.

## Co weszło (dwa commity po `c54e018`)

1. **`b213d85`** — audyt scalonego PR #44 (diff `44c80e1..c54e018`, plik po
   pliku): bramka ADR 0061 i cache-buckety ADR 0059 spójne w aplikacji,
   lustrze `.gs` i pinach; protokół PYT/1.4 zsynchronizowany; regresia
   przeliczona na `main` (858/858). **Wynik: bez zastrzeżeń.** Zapis w
   `docs/PROJECT_HISTORY.md`.
2. **`6f657b1`** — bramki startu gry multi (dopełnienie ADR 0061):
   - `uruchomGreMulti` sam zatrzymuje grę bez stacji jawnym statusem
     („stan z mostu nie niesie stacji”), zamiast polegać wyłącznie na
     walidacji wywołującego; wymaganie `nowaRozgrywka` (puste stacje =
     błąd graniczny) bez zmian;
   - nowy scenariusz UI: most oddaje grę bez stacji przed startem →
     `[R08]` w statusie, ekran gry się nie otwiera, przycisk „▶ Start gry”
     nie wisi w „Łączę z siecią” (L77), po naprawie mostu polling startuje
     grę sam;
   - piny kontraktowe dopełnienia (walidacja przed renderem, bramka,
     wymaganie stałe);
   - `?v=` i `WERSJA_SW`: `m12-162` → `m12-163`.

## ⚠ Zawieszenie pusha — do wykonania po reconnect GitHub w Arenie

Pod koniec sesji token `GH_TOKEN` wygasł (`gh auth status`: *no longer
valid*; push odrzucany pytaniem o login). Stan gałęzi: zdalna ma
**`b213d85`** (audyt — zdążył się wypchnąć), lokalne do wypchnięcia są
**`6f657b1`** (bramki) i HEAD sesji (ten handoff), wszystko na
`arena/01a0b45e-okolica`. Po reconnect właściciela:

```bash
git push origin arena/01a0b45e-okolica     # żadnego --force (ADR 0012)
gh pr view 45                               # kontrola opisu PR
```

Opis PR #45 wczytany jest w wersji roboczej — po pierwszym commicie
planowana jest aktualizacja body (pkt „bramki” z realnymi numerami commitów).

## OTWARTE — zadania właściciela (poza kodem)

1. **Po reconnect: push dwóch commitów + przegląd PR #45.**
2. Z poprzedniego handoffu (nadal aktualne): przegląd ADR 0061 (PR #44 —
   scalony; decyzja bramki do potwierdzenia w terenie), ponowne wdrożenie
   `.gs` (schemat `sieci/2`), kasowanie 3 starych plików L2, test iPhona na
   `m12-163` (nowość: multi bez stacji z mostu niestartuje + bramka ADR 0061
   z poprzedniej tury).

Kolejka: **uwagi z terenu** — jedyna praca, gdy nie ma zlecenia (L68).

## Brama jakości

| Sprawdzenie | Wynik |
| --- | --- |
| `npm test` | **860/860** |
| `npm run brama` | **EXIT 0** (testy + szablon + kontrast + budżet) |
| `npm run budzet` | **99 253 / 100 000** (rezerwa 747) |
| wersje | protokół `PYT/1.4`, szablony `PYT/1.4.0` / `PYT/1.4-nofc.0`, cache `m12-163`, `WERSJA_SW` `m12-163` |
