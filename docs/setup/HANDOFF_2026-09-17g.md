# Handoff 2026-09-17g — audyt PR #42 i higiena budżetu lektury

Gałąź sesji: `arena/01a0b144-okolica` (baza: `main` = `48003a2`). Wszystko
w jednym PR-ze: **PR #43**. Zlecenie właściciela: „kontynuujemy projekt” — bez
nowego zadania, więc sesja zrobiła obowiązkową sekwencję startową: lektura §0
→ PR → audyt poprzedniego scalonego PR-a. Kod aplikacji zmieniony tylko tam,
gdzie audyt wykazał usterkę.

## Co weszło i jak jest pokryte

| Pozycja | Zmiana | Pokrycie |
| --- | --- | --- |
| Usterka 1 (ADR 0055) | `poziomyBiezacegoSetupu()` — jedno źródło poziomów: hot-seat z listy graczy, multi z poziomu gry; użyte w `oczekiwane()`, `metaBiezacejOkolicy()`, kryteriach dopasowania paczek, `metaSesjiMulti()` i `gra-zaloz` | pin „kontrakt ADR 0055 (audyt PR #42)” w `test/kontrakt.test.js` (formuła ma jedno wystąpienie w `app.js`) |
| Usterka 2 (wdrożenie) | bump `?v=` `m12-160` → **`m12-161`** w `index.html`, `sw.js` i wszystkich importach `app/*.js` (historia i ADR-y ogłaszały 161 już 2026-09-17f — kod niósł 160) | kontrakt cache-bustingu (spójność wszystkich `?v=`) |
| Dryf dokumentów | WORKFLOW (opis setupu i wiersz o nowym poziomie), PROTOKOL §3 (nagłówek PYT/1.3) i przykłady §9, AGENTS §3 (ściąga PYT/1.3, poziomy, fact-check) | pięć martwych fraz w `test/dryf-dokumentow.test.js`: „kategoria wiekowa (7 lat / 12 lat / dorośli)”, „Nowa kategoria wiekowa”, „pytanie bez `zrodla[]`”, „PYT/1.0”, „Schemat paczki PYT/1.2” |
| Budżet lektury (L62/L79) | aneksy historyczne ADR 0009, 0010, 0015, 0020, 0022, 0044 → `docs/decisions/archive/` (6 nowych plików) ze wskaźnikami w plikach macierzystych | strażnik cytowań „ADR NNNN aneks <data>” i rejestr ADR bez zmian |

## Brama jakości

| Sprawdzenie | Wynik |
| --- | --- |
| `npm test` | **856 / 856** (było 855 — doszedł pin audytu) |
| `npm run brama` | **EXIT 0** |
| `npm run budzet` | **98 903 / 100 000** (rezerwa **1 097**; na starcie sesji było 99 983 / rezerwa 17) |
| wersje | protokół `PYT/1.3`, szablony `PYT/1.3.1` / `PYT/1.3-nofc.1`, cache **`m12-161`** (nadrobiony bump) |
| serwer podglądu | `npm run serwer` na `0.0.0.0:8000` |

CI na gałęzi: `test` **success** (`b53c7a7` — 2 commity sesji).

## Audyt PR #42 (`48003a2`) — co sprawdzono

Plik po pliku: `app/*.js` (konfig, app, protokol, rozgrywka, zestawy,
wieloosobowa), `index.html`, `sw.js`, `test/*`, `docs/*`,
`docs/setup/apps-script-repo-paczek.gs` (+66 linii: `poziomyPytan`, `poziom`
w profilu, slug trudności). Wdrożenie ADR 0055–0058 zgodne z treścią;
`profil-ustaw`/`profil-weryfikuj` w moście niosą poziom (auto-selection),
`RO-lobby/1` dalej oddaje historyczne `wiek` (pole tylko do odczytu).
Usterki 1 i 2 — wyżej; innych niezgodności nie znaleziono.

## OTWARTE — poza kodem

1. **Właściciel: ponowne wdrożenie mostu Apps Script** — teraz z polami
   `poziomyPytan` (konfiguracja gry i meta paczki), `poziom` w profilu
   i slugiem trudności; procedura `docs/setup/most-drive-instrukcja.md`
   §184-190. Bez tego cache L2 (ADR 0052 + `TOLERANCJA_KOTWICY_M=200`)
   po cichu nie odpowiada.
2. **Test terenowy na iPhonie na świeżym `?v=m12-161`:** kolejka
   A (pomarańczowe pineski zaliczonych), B (czysty ekran stacji), C1 (cztery
   ikony modeli + znaczek paczki), C2 (pulsujące „Łączę z siecią…”),
   D (po starcie lobby od razu mapa + mini-pasek), **poziomy pytań**
   (hot-seat: przełączniki przy imionach; multi: jeden poziom hosta),
   krótki prompt `PYT/1.3` (zestawienie pod stacjami, bez źródeł i daty).
3. **Rezerwa budżetu** — 1 097 tokenów; kolejny aneks terenowy zmieści się bez
   archiwizacji, ale przy 2–3 kolejnych wracamy do cięcia wg L62/L79
   (kandydaci: 0036 ~1 035, 0006 ~736, 0005 ~712, 0009 ~330, 0028 ~406).
