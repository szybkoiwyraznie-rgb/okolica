# Handoff 2026-09-14c — uwaga B, dogrywka (`arena/01a09f7d-okolica`, PR #24, m12-116)

Ten plik jest skrótem JEDNEJ sesji (AGENTS.md §0 pkt 7): stan na koniec i rzeczy
otwarte. Nie jest źródłem zasad — zasady są w `AGENTS.md`, ADR-ach, `PROTOKOL.md`
i `docs/LESSONS.md`.

## Gdzie jesteśmy

- Gałąź sesji `arena/01a09f7d-okolica`, PR **#24**, baza `325b759` (= `main`
  po PR #23). Wersja aplikacji: **m12-116** (`?v=` w `index.html` i we
  wszystkich importach `app/*.js` + `WERSJA_SW` w `sw.js`).
- Brama na koniec sesji: **793/793** testów, `npm run check` OK (oba warianty
  protokołu), `npm run audyt` — 0 naruszeń WCAG AA.
- Budżet lektury startowej: **97 388 / 100 000 tok** (rezerwa **2 612**).
- Szczegóły: `docs/PROJECT_HISTORY.md` → „Sesja 2026-09-14c”.

## Co zrobiła ta sesja (commit → co)

| Commit | Wersja | Co i gdzie zapisane |
|---|---|---|
| `8f8584d` + `dd2fa77` + `016076b` | m12-115 | **Sesja b (z remote)**: dziennik „Sesja 2026-09-14b”, aneks 2026-09-14 do ADR 0009 (O2: koniec `czyStartPoDalej`), `HANDOFF_2026-09-14b.md`. Docs-only. |
| `bbe4d86` | m12-115 | **Dogrywka 1/3 (czysta)**: `kolejnoscTrasy` (stacja 1 zawsze najbliższa startu, reszta TSP od niej) + `uporzadkujGre` (przepięcie pytań: tylko `stacja`) w `app/stacje.js`; pierścień i sieć przełączone; fałszywy komentarz o nierówności usunięty. 9 testów w `test/stacje.test.js`. |
| `264f5ad` | m12-116 | **Dogrywka 2/3 (wpięcie)**: `przyjmijZestawDoGry` i wklejka przestawiają grę w kolejność trasy (`app/app.js`); start z paczki kasuje `STAN.wynikSieci`; O3 z sesji b (martwe zdanie o panelu multi); aneks m12-116 do ADR 0005; `?v=m12-116` + `WERSJA_SW`. Testy: E2E paczki wspak, pin promptu, pin kontraktu, martwa fraza. |
| (ten commit) | m12-116 | **Dogrywka 3/3**: dziennik „Sesja 2026-09-14c” + ten handoff. |

## Decyzje, które następna sesja musi znać

1. **Reguła wejścia (ADR 0005 aneks m12-116)**: stacja 1 = najbliższa startu w metryce porządkowania, na wszystkich ścieżkach (pierścień, sieć, wklejka, paczka). Wolne TSP (`optymalnaKolejnosc`) żyje tylko jako silnik reszty trasy — gry go nie wołają wprost.
2. **Literalna nierówność właściciela nie jest regułą ogólną** (trójkąt: tylko współliniowo). Test pinuje jej intencję + równość w przypadku współliniowym. Fraza `≥ start-1 + 1-2` jest martwa w dryfie — nie wraca do kodu ani dokumentów.
3. **Pytania przy przestawieniu**: zmienia się TYLKO `p.stacja`; `id` musi zostać (głosy), `poprawna` musi zostać (indeksy odkodowane). Pytanie z `id: s1p1` i `stacja: 2` po przestawieniu to STAN PRAWIDŁOWY, nie błąd.
4. **Po zleceniu terenowym nie ma zlecenia — czekaj na uwagi z terenu** (L68, ROADMAP). Nie bierz M3.

## Rzeczy otwarte

1. **Scalenie PR #24** (decyzja właściciela) — teraz niesie też kod m12-116 (wcześniej tylko dziennik + aneks).
2. **Uwagi z terenu** — jedyna kolejka pracy (ROADMAP „Co jest otwarte”). Naprawiona gra: ponowna gra TĄ SAMĄ paczką też startuje trasą (porządkowanie przy starcie).
3. **Deployment web app u właściciela** — przy scaleniu PR (most `.gs` bez zmian od PR #19).
4. **Budżet: rezerwa 2 612 tok** — przy dużej fali dokumentowej archiwizuj aneksy ADR-ów żyjących (L62/L66).
