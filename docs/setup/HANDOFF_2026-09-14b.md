# Handoff 2026-09-14b — audyt PR #23 (`arena/01a09f7d-okolica`, PR #24, m12-115)

Ten plik jest skrótem JEDNEJ sesji (AGENTS.md §0 pkt 7): stan na koniec i rzeczy
otwarte. Nie jest źródłem zasad — zasady są w `AGENTS.md`, ADR-ach, `PROTOKOL.md`
i `docs/LESSONS.md`.

## Gdzie jesteśmy

- Gałąź sesji `arena/01a09f7d-okolica`, PR **#24**, baza `325b759` (= `main`
  po PR #23). Wersja aplikacji: **m12-115** (`?v=` ×43 w `index.html` i we
  wszystkich importach `app/*.js` + `WERSJA_SW` w `sw.js`) — bez zmian kodu
  aplikacji w tej sesji, tylko dziennik i jeden aneks ADR.
- Brama na koniec sesji: **781/781** testów, `npm run check` OK (oba warianty
  protokołu), `npm run audyt` — 0 naruszeń WCAG AA.
- Budżet lektury startowej: **96 841 / 100 000 tok** (rezerwa **3 159**).
- Szczegóły: `docs/PROJECT_HISTORY.md` → „Sesja 2026-09-14b”.

## Co zrobiła ta sesja (commit → co)

| Commit | Wersja | Co i gdzie zapisane |
|---|---|---|
| `8f8584d` | m12-115 | **Audyt PR #23**: squash `325b759` (32 pliki, +557/−209) plik po pliku. Naprawy G1/B1/C1/E1/F2/V1 zweryfikowane (logika, testy, piny kontraktu), łańcuch `?v=` pojedynczy, most `.gs` nietknięty, fala L68 spójna, live 200 + stopka `PYT/1.0`. Usterek brak; obserwacje O1 (proces: brak `HANDOFF_2026-09-14*.md` z sesji PR #23), O2 (dryf: aneks 0009 cytuje usuniętą `czyStartPoDalej`), O3 (odłożony stary komentarz o panelu multi). |
| `dd2fa77` | m12-115 | **O2 naprawione**: aneks 2026-09-14 do ADR 0009 — start odcinka po „dalej” jest bezwarunkowy (koniec `czyStartPoDalej`, zachowanie „jeden przycisk zamiast dwóch” bez zmian). Tylko dokumentacja. |
| (SHA w opisie PR #24) | m12-115 | Handoff `HANDOFF_2026-09-14b.md` (ten plik). |

## Decyzje, które następna sesja musi znać

1. **Po audycie nie ma zlecenia — czekaj na uwagi z terenu** (L68, ROADMAP). Nie bierz M3, nie przypominaj o 360 px ani o „pierwszym wdrożeniu” mostu.
2. **O1: sesja PR #23 nie zostawiła handoffu** — łańcuch `HANDOFF_2026-09-14*.md` zaczyna ten plik (dlatego sufiks `b`). Wpis dziennika sesji PR #23 jest szczegółowy, więc nic nie przepadło.
3. **O3 odłożone**: komentarz `app/app.js` („Panel multi żyje poza slotem”, panel zdjęty w m12-108) — zmiana `app/*.js` wymusiłaby podbicie `?v=`; do najbliższej fali kodowej.
4. **Serwer podglądu**: `node tools/serwer.mjs .` na 0.0.0.0:8000 działa (200, `?v=m12-115`, stopka `PYT/1.0`, `.pulsuje` negatyw i `#lobby-gracze li` w serwowanym CSS).

## Rzeczy otwarte

1. **Scalenie PR #24** (decyzja właściciela) — tylko dziennik + aneks, aplikacja bez zmian (`?v=m12-115`).
2. **Uwagi z terenu** — jedyna kolejka pracy (ROADMAP „Co jest otwarte”).
3. **Deployment web app u właściciela** — przy scaleniu PR (most `.gs` bez zmian od PR #19).
4. **Budżet: rezerwa 3 159 tok** — starczy na kilka małych aneksów; przy dużej fali dokumentowej archiwizuj aneksy ADR-ów żyjących (L62/L66).
