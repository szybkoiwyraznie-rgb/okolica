# Handoff 2026-09-13 — partia uwag terenowych A–K i M domknięta (`arena/01a0973d-okolica`, PR #19, m12-110)

Ten plik jest skrótem JEDNEJ sesji (AGENTS.md §0 pkt 7): stan na koniec i rzeczy
otwarte. Nie jest źródłem zasad — zasady są w `AGENTS.md`, ADR-ach, `PROTOKOL.md`
i `docs/LESSONS.md`.

## Gdzie jesteśmy

- Gałąź sesji `arena/01a0973d-okolica`, PR **#19**, HEAD `3d6ae93` = origin.
  Wersje aplikacji: **m12-110** (`?v=` w `index.html` i importach + `WERSJA_SW`).
- Brama na koniec sesji: **760/760** testów, `synchronizuj-szablon --check` OK
  (oba warianty protokołu), audyt kontrastu WCAG AA — 0 naruszeń.
- Budżet lektury startowej: **98 925 / 100 000 tok** (rezerwa 1 075). Struktura
  kosztu i co z tym zrobić: `AGENTS.md` §0 (akapit o progu) i LESSONS **L62**.
- Partia zgłoszeń terenowych właściciela jest DOMKNIĘTA w kodzie: A, B, C, D
  (wycofane i poprawione), E, F, G, H1, H2, I, J, K, L, M. Szczegóły per punkt:
  `docs/PROJECT_HISTORY.md` → „Sesja 2026-09-13”.

## Co zrobiła ta sesja (commit → decyzja)

| Commit | Wersja | Co i gdzie zapisane |
|---|---|---|
| `8ef27c5` | m12-101 | uwaga A: pytanie i odpowiedzi w `<details>` na stacji |
| `fac0441` | m12-102 | uwagi B+E: koniec pauzy i profili GPS — **ADR 0040** |
| `423aae9` | m12-103 | uwagi B+C: Wake Lock + jedyna przerwa (15 min bezczynności) — ADR 0040 pkt 4–5 |
| `0c527b4` | m12-104 | uwaga D po wycofaniu: brak blokady orientacji, po obrocie samo ◎ |
| `db4d619` | — | uwaga M: sygnał = dźwięk I wibracja — **ADR 0041** |
| `81e8576` | m12-105 | uwaga L: pula premii = min(3, grający − 1) — aneks ADR 0027 (kod + `.gs`) |
| `88b93a7` | m12-106 | uwaga H2: Informacje jedną małą czcionką — **ADR 0042** |
| `c0aedb8` | m12-107 | uwagi H1+I: koniec gry za ikoną ⚙ START GRY z wpisaniem TAK — **ADR 0043** |
| `ec5eff6` | m12-108 | uwaga F: odliczanie 5-4-3-2-1-START, gra jak w hotseat — **ADR 0044** + aneks ADR 0019 |
| `6339d32` | m12-109 | uwaga G: koniec gry hosta NIE kończy gry pozostałym — aneks **ADR 0019 2026-09-13b** + reguła domykania w `.gs` |
| `a041c64` | m12-110 | uwagi J+K: kart wznowienia nie ma, telefon wraca do zapamiętanej gry sam — **ADR 0045** |
| `3d6ae93` | — | budżet lektury: rejestr LESSONS skrótem, pełne opisy w `docs/LESSONS_ARCHIVE.md`; LESSONS **L62**, **L63** |

## Co dalej (nie ruszone w tej sesji)

1. **NOWY deployment web app u właściciela** — most
   `docs/setup/apps-script-repo-paczek.gs` zmienił się w tej sesji dwa razy
   (uwaga L: pula premii; uwaga G: `if (z.typ !== 'koniec' && czyKompletna(gra))`).
   Na starym adresie premia liczy się po staremu, a gra po rezygnacji ostatniego
   aktywnego gracza domknie się dopiero przy kolejnym zdarzeniu gracza, który
   jeszcze gra. Instrukcja: `docs/setup/most-drive-instrukcja.md`.
2. **Powtórka testu terenowego dwóch telefonów** (kryterium M11/M12, WORKFLOW
   §4.4) — po nowym deploymencie. Do sprawdzenia zwłaszcza: odliczanie po starcie
   u obu graczy, koniec gry hosta (u drugiego gracza gra toczy się dalej, wspólna
   tabela po domknięciu), powrót do gry po odświeżeniu BEZ banera i bez okna
   startowego, premia przy różnej liczbie grających.
3. **Budżet lektury**: rezerwa 1 075 tok = jeden ADR. Gdy próg pęknie,
   obowiązkowym zadaniem sesji jest podział ADR-ów (kandydaci: decyzje w całości
   uchylone przez późniejsze → `docs/decisions/archive/`, link w rejestrze;
   pin „ADR na dysku ↔ rejestr” czyta tylko `NNNN-*.md` w katalogu, więc
   podkatalog nie wchodzi ani w pin, ani w budżet). Potrzebna zgoda właściciela
   na to, które decyzje są historyczne.
4. **PR #19** czeka na przegląd właściciela; `main` pozostaje nietknięty
   (AGENTS.md §2: nigdy nie pushujemy i nie mergujemy do `main` z sesji).

## Pułapki potwierdzone w tej sesji

- **L59 się powtórzył w trakcie sesji** (nie tylko przy wznowieniu): sandbox
  odtworzył `.git` ze świeżego klona bazy, `git add -A` zebrał całą sesję, push
  odrzucony. Rutyna przed KAŻDYM committem: `git log --oneline -2` +
  `git ls-remote origin arena/01a0973d-okolica`; przy rozjeździe `git fetch` +
  `git reset --mixed FETCH_HEAD` (nie rusza plików) + `git diff --cached --stat`.
- **L61 (nowa warstwa a widoczność)**: reguły z klasą na `body` chowają każdy
  panel niewymieniony w `:not()`; atrapa DOM nie ma silnika CSS, więc regułę CSS
  i kolejność otwierania warstw pinuje `test/kontrakt.test.js`, nie test zachowania.
- **L62**: skracanie dokumentów zacznij od tabeli `npm run budzet` — ADR-y to
  ~65 tys. z 100 tys. tokenów, LESSONS ~14,5 tys.
- **L63**: usuwając przycisk-ujście (tu: „🗑 Nowa gra (kasuje zapis)”,
  „🗑 Porzuć zapamiętaną grę”), wypisz stany, które obsługiwał, i każdemu daj
  drogę automatyczną; przywracanie stanu na starcie testuj dwiema ścieżkami
  (localStorage synchronicznie, sieć asynchronicznie — `await oddech()`).
- **Kotwice edycji dokumentów**: WORKFLOW/README mają własne łamanie wierszy i
  cudzysłowy `„…”` zamykane prosto — kotwica musi być krótka i bez cudzysłowu,
  a przed podmianą warto pokazać dokładne wiersze (`sed -n`, `cat -A`).
- **Piny nieobecności**: nagrobki w kodzie celowo zawierają nazwy usuniętych
  funkcji i etykiet, więc pinujemy SKŁADNIĘ (`function zakonczGreMulti`,
  `akcja: 'gra-zakoncz'`, `id="karta-wznowienie"`), nie gołe frazy; frazy UI idą
  do `test/dryf-dokumentow.test.js`, którego zakres omija historię (ADR-y,
  PROJECT_HISTORY, LESSONS, handoffy) i testy.
