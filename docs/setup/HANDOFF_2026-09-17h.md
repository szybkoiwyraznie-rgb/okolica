# Handoff 2026-09-17h — audyt PR #43 (bez usterek) i start sesji

Gałąź sesji: `arena/01a0b15d-okolica` (baza: `main` = `44c80e1`). Wszystko
w jednym PR-ze: **PR #44**. Zlecenie właściciela: „kontynuujemy projekt” — bez
nowego zadania, więc sesja wykonała obowiązkową sekwencję startową: lektura §0
→ PR na starcie → audyt poprzedniego scalonego PR-a. Bez zaległości i zlecenia
sesja czeka na uwagi z terenu (AGENTS.md §2, L68).

## Co weszło i jak jest pokryte

Komisy sesji są wyłącznie porządkowe — **brak zmian kodu** (audyt skończył
się czysto).

| Pozycja | Zmiana | Pokrycie |
| --- | --- | --- |
| Start sesji | wpis otwarcia w `docs/PROJECT_HISTORY.md` (lektura §0, brama 856/856, stan `main`) | — |
| Audyt PR #43 (`48003a2..44c80e1`) | wynik w `docs/PROJECT_HISTORY.md` i w opisie PR #44; bez usterek | poniżej |

## Audyt PR #43 (sesja 2026-09-17g) — co sprawdzono, bez usterek

- **Kod**: `poziomyBiezacegoSetupu()` (`app/app.js`) — jedno źródło
  per-stacyjnych poziomów (hot-seat: lista graczy, multi: JEDEN poziom hosta),
  użyte w 5 miejscach; formuła „jeden poziom gry” istnieje dokładnie raz
  (pin „kontrakt ADR 0055 (audyt PR #42)” liczy ją globalnie i asertuje ciała
  `metaBiezacejOkolicy()` i `oczekiwane()`). `polecenieHotseat()` poza gronem
  celowo — raportuje poziomy kończonej gry z `STAN.rozgrywka`, nie bieżącego
  setupu.
- **Wdrożenie**: `?v=` `m12-160` → `m12-161` w 39 miejscach + `WERSJA_SW`
  (jedna wersja wszędzie, L29).
- **Dokumenty**: AGENTS §3 / PROTOKOL §3+§9 / WORKFLOW spójne z ADR
  0055/0057/0058 (PYT/1.3, `poziomyPytan` w RO-* i przykładach); pięć martwych
  fraz w `test/dryf-dokumentow.test.js`.
- **Archiwum**: 6 grup aneksów (ADR 0009/0010/0015/0020/0022/0044) przeniesione
  dosłownie do `docs/decisions/archive/` (tylko nagłówki dołożone), wskaźniki
  z datą w plikach macierzystych; strażnik cytowań zielony.

## Brama jakości

| Sprawdzenie | Wynik |
| --- | --- |
| `npm test` (przed i po) | **856/856** |
| `npm run brama` | **EXIT 0** |
| `npm run budzet` | **98 903 / 100 000** (rezerwa **1 097** — zmierzony 2026-09-17h po zmianach dokumentów; bez zmian od 2026-09-17g) |
| wersje | protokół `PYT/1.3`, szablony `PYT/1.3.1` / `PYT/1.3-nofc.1`, cache `m12-161` |
| CI na scalonym `44c80e1` | `CI` success, `Pages` success |

## OTWARTE — poza kodem (bez zmian od 2026-09-17g)

1. **Właściciel: ponowne wdrożenie mostu Apps Script** — z polami
   `poziomyPytan` (konfiguracja gry i meta paczki), `poziom` w profilu
   i slugiem trudności; procedura `docs/setup/most-drive-instrukcja.md`.
   Bez tego cache L2 (ADR 0052) po cichu nie odpowiada.
2. **Test terenowy na iPhonie na `?v=m12-161`**: kolejka A (pomarańczowe
   pineski), B (czysty ekran stacji), C1 (cztery ikony modeli + znaczek),
   C2 (pulsujące „Łączę z siecią…”), D (po starcie lobby od razu mapa +
   mini-pasek), poziomy pytań (hot-seat: przełączniki przy imionach; multi:
   jeden poziom hosta), krótki prompt PYT/1.3 (zestawienie pod stacjami,
   bez źródeł i daty).
3. **Rezerwa budżetu** — 1 097 tokenów (handoff leży poza budżetem — pozycja
   7, narzędzie liczy 1–6); kolejny aneks terenowy zmieści się bez
   archiwizacji, kandydaci do cięcia wg 2026-09-17g: 0036 ~1 035,
   0006 ~736, 0005 ~712.
