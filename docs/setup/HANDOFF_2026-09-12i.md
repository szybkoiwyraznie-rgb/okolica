# Handoff 2026-09-12i — po sesji `arena/01a0970a-okolica` (PR #18, m12-94)

> Dla następnej sesji. Czytaj razem z `AGENTS.md` §0 i `docs/PROTOKOL.md`.
> Poprzedni handoff (`HANDOFF_2026-09-12h.md`) opisuje PR #17 (P10, backfill
> `.gs`) — wdrożenie `.gs` nadal po stronie właściciela (patrz niżej).

## Gdzie jesteśmy

- **Gałąź:** `arena/01a0970a-okolica` → **PR #18** (baza `main`), wszystko
  wypchnięte. Przed `gh pr create` zawsze `gh pr list --state open`.
- **Wersja:** `?v=m12-94` (`index.html` + WSZYSTKIE importy `app/*.js`),
  `WERSJA_SW = 'm12-94'` (`sw.js`) — spójne (kontrakt).
- **Brama (spisana z outputu po ostatnim commicie):** `npm test` →
  **733/733**, `npm run check` OK, audyt kontrastu 0 naruszeń.
- Commity: H/1 audyt PR #17 → PROJECT_HISTORY (`fdf68f4`); H/2 kod +
  testy + `?v` (`4bca46e`); H/3 dokumenty (`9d58f7a`); H/4 ten plik +
  wpis historii (poniżej).

## Zadanie H — akcja „Pomiń odcinek” usunięta, odczyt starych zapisów zostaje

Właściciel: „Pomiń odcinek (tylko w drodze)” to pozostałość bez sensu.
Kontrakt na przyszłość — **NIE ruszać bez zlecenia:**

- **ZNIKŁO:** przycisk `#przycisk-pomin-stacje`, `pominStacje()`,
  `pominStacjeGry()`, kody **G11/G13** (numery ZAJĘTE, nie wracają do puli),
  przypomnienia o pomijaniu w P03/P04/P08 i w statusie GPS.
- **ZOSTAŁO (stare zapisy muszą być czytelne):** stan `pominiety`,
  zdarzenie `pominiecie`, licznik `pominietaStacje`, strażnik G14, etykiety
  wyniku, walidacja zapisów. Silnik NIC nie tworzy, ale wszystko czyta.
- **Nowe wyjście z nieosiągalnej stacji:** „■ Zakończ grę” (komunikaty
  odsyłają wprost). Testy: `zamknijStacje` (pełna ścieżka UI),
  `jakoPominieta` (odtworzony stary zapis), piny nieobecności w silniku,
  aplikacji i kontrakcie. Aneksy: ADR 0015/0004/0029/0036.

## Pułapki potwierdzone w tej sesji

- **ADR 0015: pomijanie to pkt 3, nie pkt 2.** Kod i ADR 0004/0029 mówiły
  „pkt 2” — sprostowane w aneksach H. Przy cytowaniu ADR liczyć punkty
  z pliku, nie z komentarzy w kodzie.
- **`nastepna-stacja` auto-startuje odcinek w solo** (`czyStartPoDalej`).
  Testy po „Następnej stacji” NIE klikają startu (podwójny start = G03
  i zapalony `bledy-gra`) — pattern w `zamknijStacje`.
- **Edycja heredokiem + polskie cudzysłowy:** w komentarzach żyją
  historyczne artefakty `„X"` (ASCII-zamykacz) i `„magicznie\"`
  (z backslashem). Podmiany literalne dopasowywać do BAJTÓW
  (`unicode_escape`), nie do „sensu”; asercja `count == 1` przed zapisem
  ratuje drzewo (skrypt H padał 4×, drzewo ani razu nie ucierpiało).
- **Wynikowa zasada z audytu PR #17 (O1/O2):** wersję `?v=` i liczbę
  testów w dokumentach spisywać z drzewa PO ostatnim commicie.

## Otwarte dla właściciela

1. **WDROŻENIE `.gs`** („Wdróż → Nowa wersja”) — z handoffu 12h, bez zmian:
   jedno wdrożenie niesie ranking (ADR 0039) i naprawę starych paczek.
2. Potwierdzenie terenowe P10 na iPhonie.
3. Kamienie M3–M8, M10–M12 (kryteria terenowe).

## Co dalej w następnej sesji

- PR #18 czeka na review/merge. Po scaleniu: blok startowy (AGENTS §2).
- Brak zlecenia → przegląd tego handoffu + historii sesji 12h.
