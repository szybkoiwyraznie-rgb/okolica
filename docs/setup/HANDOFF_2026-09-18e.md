# Handoff 2026-09-18 (e) — sesja `arena/01a0b4de-okolica`, PR #46

Stan gałęzi: `arena/01a0b4de-okolica` (PR #46, draft, baza `main`).
Brama: `npm test` 864/864, `npm run brama` EXIT 0, budżet lektury
99 785 / 100 000 (rezerwa 215 — następne aneksy ADR najpierw archiwizować
albo skracać, L62/L79). Wersja cache `?v=`/`WERSJA_SW`: `m12-166`.

## Co się wydarzyło w tej sesji

1. **Audyt PR #45** — bez zastrzeżeń (wpis w historii).
2. **Uwaga terenowa 1** — ekran stacji bez zdublowanej czerwonej karty
   błędów sieci (stop/pierścień są pełnymi komunikatami; diagnostyka
   w ⓘ Informacje). ADR 0061 aneks.
3. **Pytanie-projekt właściciela o cache sieci** — najpierw odpowiedź
   (L1/L2/Overpass, koszyki, pokrycie), potem decyzja właściciela:
   rozszerzyć L1 analogicznie do L2.
   - `odczytajCacheSieci`: pełny skan `okolica:sieci:*`, geometria
     rozstrzyga — wpis z sąsiedniej komórki/szerszego koszyka obsługuje
     grę bez mostu i bez Overpass;
   - dokarmienie L1 z L2 zachowuje kotwicę i koszyk ŹRÓDŁA (prawdziwy
     zasięg danych działa w L1);
   - ADR 0059 aneks 2026-09-18; testy (2 nowe) + weryfikacja live
     headless Chromium (gra realna, wpisy spoza komórki, 0 wołań sieci).

## Zadania właściciela (narastająco)

- **Wdrożyć `.gs` ze schematem `sieci/2`** — bez tego L2 nie pokrywa
  wpisów krzyżowych na innych telefonach (L1 po tej sesji pokrywa je
  na tym samym telefonie bez mostu).
- Skasować 3 stare pliki L2 na Drive.
- Test iPhona na `m12-166`: jawny stop bez czerwonej karty, etykieta
  „🧒 Dziecko”, a teraz też: druga gra w szerszej okolicy nie pyta
  Overpass (status „z pamięci telefonu”).

## Uwagi dla następnej sesji

- Po resecie sandboxa `/home/user/.narzedzia` (chromium headless) znika —
  przepis ENVIRONMENT §4.1 odtwarza się w ~15 s; serwer `npm run serwer`
  też trzeba restartować. Triki live: `#przycisk-start-zacznij`, seed
  `okolica:gracze`, GPS przez `evaluateOnNewDocument`; skrypty
  weryfikacji leżały w `.narzedzia` (stop-bez-karty, cache-cross-cell) —
  po resecie napisać od nowa wg tego handoffu.
- Lokalne `.git` po resecie może być cofnięte do bazy sesji, choć drzewo
  i origin są świeże — leczy `git fetch origin <gałąź>` + `git reset
  FETCH_HEAD` (mixed), NIGDY `--hard` (zabiłoby niezacommitowane zmiany).
