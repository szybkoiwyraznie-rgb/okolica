# Handoff 2026-09-14d — audyt PR #24 i naprawa D1 (`arena/01a09fc9-okolica`, PR #25, m12-117)

Ten plik jest skrótem JEDNEJ sesji (AGENTS.md §0 pkt 7): stan na koniec i rzeczy
otwarte. Nie jest źródłem zasad — zasady są w `AGENTS.md`, ADR-ach, `PROTOKOL.md`
i `docs/LESSONS.md`.

## Gdzie jesteśmy

- Gałąź sesji `arena/01a09fc9-okolica`, PR **#25**, baza `ea3cf8f` (= `main`
  po PR #24). Wersja aplikacji: **m12-117** — `?v=` w 43 miejscach (2 w
  `index.html`, 41 w importach `app/*.js`) + `WERSJA_SW` w `sw.js`.
- Brama na koniec sesji: **794/794** testów, `npm run check` OK (oba warianty
  protokołu: 4015 / 4263 znaki), `npm run audyt` — 0 naruszeń WCAG AA.
- Budżet lektury startowej: **97 990 / 100 000 tok** (rezerwa **2 010**).
- Szczegóły: `docs/PROJECT_HISTORY.md` → „Sesja 2026-09-14d”.

## Co zrobiła ta sesja (commit → co)

| Commit | Wersja | Co i gdzie zapisane |
|---|---|---|
| `22169fe` | m12-116 | **Audyt PR #24** (bez zmian kodu): zweryfikowane `kolejnoscTrasy`/`uporzadkujGre` (remis po indeksie, podmacierz po starych indeksach, fallback na kreskę), przepięcie pytań przez `Map` po STARYM `id`, kolejność przed `wyslijZestawNaDrive()` i przed lobby, `walidujPaczke` przed porządkowaniem, round-trip `zapakujPaczke`/`zbudujPlikZestawu`. Znaleziony defekt **D1** + zmierzona osiągalność (sonda: 4740 pozycji z rozjazdem metryk na fixture `overpass-centrum`). |
| `b41dfe0` | m12-117 | **Naprawa D1**: test reprodukujący (tożsamość stacji 1) + metryka drogowa w `sprawdzOdpowiedz` (`dystansStart` + `macierz`, kasowanie `wynikSieci` przy przestawieniu); pin kontraktu przepisany; aneks m12-117 do ADR 0005; L69 (+ archiwum); `?v=m12-117` + `WERSJA_SW`. |
| (ten commit) | m12-117 | Dziennik „Sesja 2026-09-14d” (część o naprawie) + ten handoff. |

## Decyzje, które następna sesja musi znać

1. **Metryka porządkowania jest jedna** (ADR 0005 aneks m12-117): ta, którą wybrano stacje. Nowe miejsce wołające `uporzadkujGre`/`kolejnoscTrasy` musi podać `dystansStart` + `macierz` ze źródła stacji, a przy zmianie kolejności skasować `STAN.wynikSieci` — `dystanseOdcinkowM` czyta macierz POZYCJAMI.
2. **Start z paczki repozytorium zostaje na kresce** (celowo, nie luka): plik zestawu niesie `{lat, lon, opis}` bez danych drogowych, więc metryki drogowej tam nie ma (ADR 0017 pkt 7).
3. **Test porównawczy pinuje tożsamość, nie miarę**: 485 m było „poprawną" liczbą z cudzego odcinka — asercja na długość by przeszła (L69).
4. **Po audycie bez zlecenia — czekaj na uwagi z terenu** (L68, ROADMAP). Nie bierz M3.

## Rzeczy otwarte

1. **Scalenie PR #25** (decyzja właściciela) — niesie naprawę D1 i wersję m12-117.
2. **Uwagi z terenu** — jedyna kolejka pracy (ROADMAP „Co jest otwarte”). Do sprawdzenia w terenie: gra siecią drogową + wklejka (stacja 1 = najbliższa DROGĄ, dystanse odcinków własne, etykieta „drogą" tylko przy danych sieciowych).
3. **Deployment web app u właściciela** — przy scaleniu PR (most `.gs` bez zmian od PR #19).
4. **Budżet: rezerwa 2 010 tok** — przy następnej fali dokumentowej archiwizuj aneksy ADR-ów żyjących (L62/L66); sam aneks m12-117 + L69 kosztowały 602 tok.
