# Handoff 2026-09-16c — audyt PR #36 (gałąź `arena/01a0aa68-okolica`, m12-149)

## Gdzie jesteśmy

Sesja bez uwag z terenu (zlecenie „kontynuujemy projekt”) wykonała to, co każe
`AGENTS.md` §2 pkt 2: **audyt poprzedniego scalonego PR (#36, squash `7227617`
na `7974981`, 19 plików, +330/−77)**. Kamienie M0–M12 pozostają zamknięte jako
zakres kodu (LESSONS L68); kolejka pracy to uwagi z testów terenowych.

Brama startowa i bieżąca: `npm test` **829/829**, budżet **99 963 / 100 000**
(rezerwa 37), cache w `main` = `m12-149`.

## Wynik audytu PR #36

**Zgodne, bez zastrzeżeń:**
1. **Naprawa przycisku startu lobby:** `startLobby()` przywraca stan
   `disabled`, etykietę i klasę `pulsuje` w bloku `finally`. Stan „w locie”
   nie przecieka do następnej gry, więc host może założyć i wystartować kolejną
   grę w tej samej sesji strony bez konieczności przeładowania.
2. **Test odtwarzający:** `test/wieloosobowa-ui.test.js` pokrywa scenariusz
   dwóch kolejnych gier w jednej sesji strony (gra 1 → koniec z ręki → setup →
   gra 2) i przechodzi stabilnie.
3. **Porządki w `app/app.js`:** usunięto martwą zmienną `const teraz`, usunięto
   martwy import `OGRANICZENIA`, uściślono komentarz `czyscPlikiTymczasowe()`.
4. **Spójność cache-bustingu:** wersja `m12-149` wprowadzona konsekwentnie
   w `index.html`, wszystkich plikach `app/*.js` oraz `sw.js`. Brak starych
   odwołań do `m12-148`.
5. **Dyscyplina dokumentacji i budżetu:** dodano L77 w `docs/LESSONS.md`
   i `docs/LESSONS_ARCHIVE.md`; szkielety wcześniejszych lekcji (L71, L72, L74,
   L76) zostały skrócone, dzięki czemu budżet lektury startowej mieści się
   w limicie (99 963 / 100 000, rezerwa 37 tokenów).

**Znalezione defekty:** brak.

## Brama jakości

| Sprawdzenie | Wynik |
| --- | --- |
| `npm test` | **829 / 829** |
| `npm run check` | OK — szablon §2 (3 603 znaki), §2.2 (3 738) |
| `npm run audyt` (WCAG) | 0 naruszeń |
| `npm run zasieg-mostu` | 97,7% (850/870) |
| `npm run budzet` | **99 963 / 100 000** (rezerwa 37) |
| wersje | protokół `PYT/1.1`, szablony `PYT/1.1.2` / `PYT/1.1-nofc.2`, cache `m12-149` |

## Co właściciel ma zrobić

1. **Scalić PR tej sesji** (squash). Most Apps Script bez zmian — nie trzeba
   wgrywać nowej wersji.
2. **Kolejne testy w terenie:** aplikacja oczekuje na nowe uwagi i obserwacje
   z testów terenowych właściciela na telefonie (iPhone).

## Otwarte

- Kolejka pracy: **pusta** — kamienie M0–M12 zamknięte, czekamy na uwagi
  z terenu (LESSONS L68).
