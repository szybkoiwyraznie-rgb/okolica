# Handoff 2026-09-14h — uwagi terenowe A–E (gałąź `arena/01a0a155-okolica`, PR #28, m12-121)

Ten plik jest skrótem JEDNEJ sesji (AGENTS.md §0 pkt 7): stan na koniec i rzeczy
otwarte. Nie jest źródłem zasad — zasady są w `AGENTS.md`, ADR-ach, `PROTOKOL.md`
i `docs/LESSONS.md`.

## Gdzie jesteśmy

- Gałąź sesji `arena/01a0a155-okolica`, PR **#28**, baza `9c4a40c` (= `main`
  po PR #27). Wersja aplikacji: **m12-122** — `?v=` w 43 miejscach +
  `WERSJA_SW` w `sw.js`.
- Brama na koniec sesji: **812/812** testów zielonych (było 809).
  `npm run budzet` — 98 961 / 100 000 (rezerwa 1039 tok; doszły ADR 0046
  i ADR 0047, aneksy ADR 0005 m12-115–119 poszły do archiwum wg L62/L66).
- Właściciel potwierdził w terenie fix m12-119/120 („Na razie fix działa!!!”).
- Szczegóły: `docs/PROJECT_HISTORY.md` → „Sesja 2026-09-14h”.

## Co zrobiła ta sesja

| Commit | Wersja | Co i gdzie zapisane |
|---|---|---|
| `4197106` | — | **Uwaga 0 / W1**: `mapa stacje.jpg` usunięty (decyzja właściciela). |
| `e88d958` | (m12-121) | **B1+B2 czekanie ma puls**: `przyjmijZestawDoGry` oczekuje na `zalozGreMulti`, przycisk „⏳ Ładuję paczkę…” pulsuje do lobby; statusy POST-ów z `czeka:true`; lobby „Pobieram listę gier…” bez Drive + puls; fraza w `test/dryf-dokumentow.test.js`. |
| `baaf4fc` | (m12-121) | **C / ADR 0046**: promień jest kryterium dopasowania paczek (równość); `app/zestawy.js`; wskazówka w aneksie ADR 0024 (bez zmiany jego treści). |
| `1388d0e` | (m12-121) | **D**: w Wyścigu żadna pinezka nie jest „aktywna” (`aktywna: null` przy `tryb === wyscig`); zamknięte przez gracza mają `pinezka-zaliczona`; trasa bez zmian. |
| `2ddbf01` | (m12-121) | **E (poważny)**: stara gra sieciowa odradzała się z pollingu po „Wróć na początek”. Fix: `m.zrezygnowano` + strażnik w `onStanGryMulti`; `wrocNaPoczatek` kończy kontekst sieciowy (+ flush zaległych zdarzeń). |
| `3a03a94` | m12-121 | Bump `?v=` (15 plików) + `WERSJA_SW`. |
| `c245fe7` | — | Archiwum: aneksy ADR 0005 m12-115–117 (budżet §0 z powrotem pod 100k); testy kontraktowe czytają archiwum. |
| `85caf3f` | m12-122 | **A / ADR 0047** (decyzja właściciela: „blokuj”): strona nie jest szczypalna poza mapą — `gesturestart`/`gesturechange` poza `.mapa` dostają `preventDefault`; ADR 0011 ze znacznikiem; archiwum rozszerzone do m12-115–119. |
| `fa5498c` | — | Dziennik 2026-09-14h + ten handoff. |

## Decyzje, które następna sesja musi znać

1. **E — niezmienność**: gra, z której TEN telefon wyszedł (rezygnacja),
   NIGDY nie odradza się z pollingu (`m.zrezygnowano` + strażnik). Rezygnacja
   dalej zostawia synchronizację żywą CELU (wspólna tabela na ekranie wyników),
   ale „🏠 Wróć na początek” ją kończy. Nie usuwać żadnej z dwóch warstw.
2. **C — ADR 0046 odwraca fragment aneksu ADR 0024**: promień dopasowania =
   równość paczka↔setup, bez tolerancji. Fixtury paczek w testach multi
   (`zasiejZestaw`) muszą liczyć promień jak setup telefonu
   (`promienZCzasuGry`, 60 min, piesza, pytaniaNaStacje=1 dla promienia).
3. **D — kolory pinezek**: wyróżnienie „aktywnej” tylko tam, gdzie kolejność
   narzuca trasa (Wspólna Trasa); w wyścigu kolor mają wyłącznie stacje
   zamknięte przez tego gracza.
4. **B — wszystkie czekania pulsują negatywem** (`.pulsuje`, `czeka:true`);
   komunikaty czekania nie wymieniają „mostu Drive”.

## Otwarte

- Uwaga A DOMKNIĘTA w tej sesji (ADR 0047, commit `85caf3f`): właściciel
  wybrał blokadę — strona nie jest szczypalna poza mapą. W terenie do
  sprawdzenia: przywrócenie karty przybliżonej nie powinno już dawać
  „ściśniętej” strony (mapa dalej szczypie normalnie).
- Kolejka pracy: uwagi z terenu po m12-122 (L68).
