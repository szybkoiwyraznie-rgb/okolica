# Handoff 2026-09-12h — po sesji `arena/01a096bd-okolica` (PR #17, m12-92)

> Dla następnej sesji. Czytaj razem z `AGENTS.md` §0 i `docs/PROTOKOL.md`.
> Poprzedni handoff (`HANDOFF_2026-09-12g.md`) opisuje buga G — nadal
> aktualny; ten plik dodaje uwagi terenowe (P10, stare paczki).

## Gdzie jesteśmy

- **Gałąź:** `arena/01a096bd-okolica` → **PR #17** (baza `main`), wszystko
  wypchnięte. Przed `gh pr create` zawsze `gh pr list --state open`.
- **Wersja:** `?v=m12-92` (`index.html` + WSZYSTKIE importy `app/*.js`),
  `WERSJA_SW = 'm12-92'` (`sw.js`) — spójne (kontrakt).
- **Brama:** `npm test` → **733/733** (5 nowych), `npm run check` OK,
  `zasieg-mostu` 97.8 proc., budżet lektury **83 322/100 000**.
- Commity: audyt PR #16 → PROJECT_HISTORY; P10 (kod + pin + `?v`) z
  dopełnieniem `2179d98` (L7 — commit nie wziął testu); aneks ADR 0004;
  backfill `.gs` + 5 testów; aneks ADR 0017 + LESSONS L57; ten plik.

## Uwaga terenowa 1 — P10: zamknij i otwórz aplikację (m12-92)

Właściciel: wskazane w komunikacie „Ustawienia → Prywatność i bezpieczeństwo
→ Usługi lokalizacji + odśwież strony” **nie działa**; działa zamknięcie
aplikacji (karty przeglądarki) i ponowne otwarcie. Treść P10 zmieniona
(ADR 0004 aneks m12-92), pin przepisany na nową formę (L55). Mechanizm
watchdoga (m12-91) bez zmian. **Otwarte:** potwierdzenie na iPhonie —
jeśli cisza dalej, prosimy o treść „próba N” w zgłoszeniu.

## Uwaga terenowa 2 — stare paczki: backfill faktycznych tematów (m12-92)

Właściciel: paczki sprzed 2026-09-11 odrzucane („tematy spoza setupu”)
choć pytań z tych tematów w nich nie ma; „to miało być już naprawione”.
Przyczyna (L57): naprawa z 2026-09-11 objęła meta nowych wysyłek,
migrację rejestru lokalnego i regułę dopasowania — nie pliki na Drive
(indeks przepisywał `meta.tematy` wprost), a „odświeżenie ponowną wysyłką”
było martwe (most nie nadpisuje duplikatu, ADR 0028).

Naprawa: `tematyPytanZestawu()` + `budujIndeks()` w
`docs/setup/apps-script-repo-paczek.gs` — indeks dopisuje FAKTYCZNE tematy
pytań z dekoderowanego kontenera (wzorzec B19: plik na Drive nietknięty);
klient bez zmian; 5 testów (L33), w tym regresja kliencka.

**Odpowiedź na pytanie właściciela („usunąć czy naprawić?”): NIC nie
trzeba usuwać** — po wdrożeniu `.gs` stare paczki zaczną się dopasowywać
automatycznie (indeks dopisuje fakty przy każdym odczycie). Usuwanie
zostaje opcją awaryjną (kasowanie pliku w katalogu zaakceptowanych na
Dysku — indeks to odczytuje na żywo).

## Pułapki potwierdzone w tej sesji

- **L7 drugi raz:** `git add index.html app/ sw.js` nie wziął
  `test/pozycja.test.js` — wypchnięty commit miał czerwoną bramę,
  dopełnienie `2179d98`. Zawsze: `git status --short` przed commitem i
  katalogi w całości przy zmianie modułu.
- Komunikaty commitów z `%` w treści (np. „97.8%”) — `printf` je zjada;
  pisać do pliku heredokiem, nie printfem.
- Nowe testy: `assert.deepEqual` dla tablic (equal = porównanie referencji).

## Otwarte dla właściciela

1. **WDROŻENIE `.gs`** („Wdróż → Nowa wersja”) — jedno wdrożenie niesie
   RAZEM ranking (ADR 0039) i naprawę starych paczek (backfill m12-92).
   Do tego czasu: stare paczki odrzucane, ranking nieczytelny.
2. Potwierdzenie terenowe P10 na iPhonie.
3. Kamienie M3–M8, M10–M12 (kryteria terenowe).

## Co dalej w następnej sesji

- Brak zlecenia → blok startowy (AGENTS §2): bramy, audyt PR #17 po
  scaleniu, przegląd tego handoffu.
- Wdrożenia `.gs` nie dopytywać i nie napychać do kolejnych zmian — to
  jednorazowa czynność właściciela (ADR 0020 pkt 5); zgłoszono w PR #17
  i w tym pliku.
