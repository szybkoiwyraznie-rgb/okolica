# Handoff 2026-09-16d — audyt PR #37 (gałąź `arena/01a0aac7-okolica`, PR #38, m12-149)

## Gdzie jesteśmy

Sesja bez uwag z terenu (zlecenie „kontynuujemy projekt”) wykonała to, co każe
`AGENTS.md` §2 pkt 2: **audyt poprzedniego scalonego PR (#37, squash `31dbbf4`
na `7227617` — 2 pliki, +93)**. Kamienie M0–M12 pozostają zamknięte jako zakres
kodu (LESSONS L68); kolejka pracy to uwagi z testów terenowych.

Brama startowa i bieżąca: `npm test` **829 / 829**, budżet **99 963 / 100 000**
(rezerwa 37), cache w `main` = `m12-149`.

## Wynik audytu PR #37 (docs-only)

**Zgodne, bez zastrzeżeń:**
1. **Brama z handoffu 16c** — powtórzona w tej sesji zgodna w całości:
   `npm test` 829/829, `npm run check` OK (szablon §2 — 3 603 znaki; §2.2 —
   3 738), audyt WCAG AA 0 naruszeń, zasięg mostu 97,7% (850/870), budżet
   99 963 / 100 000.
2. **Naprawa z PR #36 nadal stoi** — `startLobby()` przywraca `disabled`,
   etykietę i klasę `pulsuje` w `finally`; test dwóch gier w jednej sesji
   strony przechodzi (`test/wieloosobowa-ui.test.js`); L77 jest w
   `docs/LESSONS.md` i w `docs/LESSONS_ARCHIVE.md`.
3. **Porządki z PR #36** — brak martwej zmiennej w `renderujInformacjeMulti()`,
   brak importu `OGRANICZENIA` w `app/app.js`, komentarz
   `czyscPlikiTymczasowe()` opisuje wyłącznie to, co kod robi.
4. **Statystyki scalenia PR #36** — 19 plików, +330/−77; brak resztek
   `m12-148`; brak fraz „czeka na scalenie” w nośnikach żywych.

**Znalezione defekty:** jedna nieścisłość zapisu — wpis 16c podawał godzinę
scalenia PR #36 jako 13:10 UTC, a źródła mówią **13:28:55Z** (`gh pr view 36`,
`mergedAt`; committer date commita `7227617` = 15:28:54+02:00). Poprawione
w miejscu na „13:28 UTC”.

## Uwagi terenowe A (ta sama sesja, po audycie) — krok 5 „Wklej odpowiedź modelu”

| Punkt | Co zrobione | Cache |
| --- | --- | --- |
| A(a) „usuń cały tekst przed polem” | akapit instrukcji usunięty z `index.html`; pin kontraktu pilnuje PUSTKI między nagłówkiem a polem (uczciwość o jawnej paczce: karta prywatności + README) | m12-150 |
| A(b) „guzik »Wklej ze schowka« w ogóle nie działa” | przycisk i jego nasłuch USUNIĘTE — na iPhonie `readText()` nie oddaje treści, a drugiej drogi czytania schowka nie ma; zostaje wklejenie palcem (waliduje samo). ADR 0006 **aneks 2026-09-16d**, piny kontraktu i dwie martwe frazy w strażniku dryfu | m12-151 |
| A(c) „artefakt z poprzedniej gry — »Paczka przyjęta (bez fact-checku)«” | `wyczyscEkranPaczki()`: karta wyniku, nagłówek i `#wklejka-status` czyszczone przy WEJŚCIU na krok 5 i na końcu gry; `#status` startuje tam pusty. Test: dwie gry w jednej sesji strony z odrzuconą paczką w pierwszej | m12-152 |

- **LESSONS: L78** — ekran kroku czyść przy WEJŚCIU (pełny opis w `LESSONS_ARCHIVE`).
- **Budżet lektury** był przekroczony falą A (100 328 / 100 000) → aneksy ADR 0006
  z 2026-09-09 (trzecia tura), 2026-09-15 i 2026-09-15d przeniesione do
  `docs/decisions/archive/` (pliki żywe niosą wskaźnik z datami). Budżet:
  **99 807 / 100 000** (rezerwa 193).
- **Weryfikacja live** (Chromium, 360 × 740, serwer Areny): A(a) zero tekstu między
  nagłówkiem a polem; A(b) jedyny przycisk kroku 5 to „← prompt”; A(c) po odrzuceniu
  karta widoczna, po powrocie na krok 5 `display: none` i puste komunikaty — 5/5.

## Brama jakości

| Sprawdzenie | Wynik |
| --- | --- |
| `npm test` | **829 / 829** |
| `npm run check` | OK — szablon §2 (3 603 znaki), §2.2 (3 738) |
| `npm run audyt` (WCAG) | 0 naruszeń |
| `npm run zasieg-mostu` | 97,7% (850/870) |
| `npm run budzet` | **99 963 / 100 000** (rezerwa 37) |
| wersje | protokół `PYT/1.1`, szablony `PYT/1.1.2` / `PYT/1.1-nofc.2`, cache `m12-152` (A(a) → m12-150, A(b) → m12-151, A(c) → m12-152) |
| CI (GitHub Actions, PR #38) | zielone — `test` 1 m 23 s (run `35116124937`) |

## Co właściciel ma zrobić

1. **Scalić PR tej sesji** (squash). Most Apps Script bez zmian — nie trzeba
   wgrywać nowej wersji.
2. **Kolejne testy w terenie** — aplikacja czeka na nowe uwagi i obserwacje
   z testów na telefonie (iPhone); w kolejce bez zmian: paczka z promptu
   `PYT/1.1.2` (zasada 7, równa liczba pytań na stację, kotwiczenie) oraz
   polecenia z PR #35.

## Otwarte

- Kolejka pracy: **pusta** — kamienie M0–M12 zamknięte, czekamy na uwagi
  z terenu (LESSONS L68).
- **Do decyzji właściciela (znalezione przy weryfikacji live, poza zakresem
  audytu):** przyciski `.przycisk-stopka` w stopce ekranu ⓘ Informacje mają
  24 px wysokości, a ADR 0011/0042 obiecują ≥ 44 px — opis i warianty naprawy:
  `docs/BACKLOG.md` → **B26**. Sesja kodu nie ruszała (zakres: audyt PR #37
  i oczekiwanie na uwagi z terenu).
