# Handoff 2026-09-17b — audyt PR #40, archiwizacja budżetu lektury, oczekiwanie na uwagi

## Gdzie jesteśmy

Sesja rozpoczęta od zlecenia „kontynuujemy projekt". Zgodnie z `AGENTS.md` §2:
1. PR gałęzi sesji istnieje przed kodowaniem — **PR #41**.
2. Wykonano pełny audyt poprzedniego scalonego PR (#40, commit `d07abb0`) —
   wynik w opisie PR #41 i w `docs/PROJECT_HISTORY.md` (sekcja 2026-09-17b).
3. Wykonano **obowiązkowe zadanie sesji**: budżet lektury był przekroczony
   (101 082 / 100 000), więc sześć aneksów ADR przeniesiono dosłownie do
   `docs/decisions/archive/aneksy-*.md` z notami wiążącymi w ADR-ach
   (0003, 0019, 0026, 0030, 0035, 0043).
4. Trwały fix procesu: kontrola budżetu weszła do `npm run brama` (pin
   w `test/kontrakt.test.js`), a reguła jest zapisana jako LESSONS L79.

Dwa commity na gałęzi, oba wypchnięte: `9f0acf3` (artefakty cudzysłowów
w komentarzach i pinie + bump `m12-157`) i `1219d6b` (archiwizacja budżetu +
budżet w bramie + L79).

## Brama jakości (stan na 2026-09-17b, po ostatniej zmianie dokumentów)

| Sprawdzenie | Wynik |
| --- | --- |
| `npm test` | **843 / 843** |
| `npm run check` | OK — szablon §2 (3 603 znaki), §2.2 (3 738) |
| `npm run audyt` (WCAG) | 0 naruszeń (jasny i ciemny motyw) |
| `npm run budzet` | **97 942 / 100 000** (rezerwa 2 058) — teraz także w `npm run brama` |
| wersje | protokół `PYT/1.1`, szablony `PYT/1.1.2` / `PYT/1.1-nofc.2`, cache `m12-157` |
| serwer podglądu | `npm run serwer` na `0.0.0.0:8000` |

CI na PR #41: **zielone** (`test` SUCCESS dla `9f0acf3`, `1219d6b`
i `0321407`).

Zmiana kodu z tej sesji jest **neutralna dla zachowania** (artefakty w zapisie
źródłowym: `\"` w literale daje ten sam tekst co `"`); bump `?v=` i `WERSJA_SW`
podniesiony zgodnie z AGENTS.md §7, bo zmieniają się pliki `app/*.js`.

## Otwarte (stan na koniec sesji)

1. **Wdrożenie mostu Apps Script** przez właściciela (wkleić
   `docs/setup/apps-script-repo-paczek.gs`, Wdróż → nowa wersja; procedura
   `docs/setup/most-drive-instrukcja.md`, sekcja o cache L2 sieci z ADR 0052).
   Bez tego wspólny cache sieci L2 nie odpowiada, a aplikacja po cichu jedzie do
   Overpass.
2. **Kolejne testy w terenie** — aplikacja czeka na nowe uwagi i obserwacje
   z telefonu (iPhone).
3. **Rezerwa budżetu to 2 058 tokenów** — wystarczy na około jeden aneks
   terenowy; przekroczenie złapie teraz `npm run brama`, a kolejność działania
   (najpierw archiwizacja, potem nowa treść) opisuje LESSONS L79.

## Co właściciel ma zrobić

1. Kontynuować testy w terenie i zgłaszać uwagi.
2. Wdrożyć nową wersję Apps Script (cache L2 sieci, ADR 0052), jeśli jeszcze
   tego nie zrobił po sesji 2026-09-16e.
