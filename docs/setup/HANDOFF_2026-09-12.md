# Handoff 2026-09-12 — po sesji `arena/01a09489-okolica` (PR #13, m12-78)

> Dla następnej sesji. Czytaj razem z `AGENTS.md` §0 i `docs/PROTOKOL.md`.
> Stan faktów poniżej odtworzono z kodu na HEAD tej gałęzi, nie z notatek.

## Gdzie jesteśmy

- **Gałąź:** `arena/01a09489-okolica` → **PR #13** (baza `main`).
- **Wersja:** `?v=m12-78` (42 miejsca), `WERSJA_SW = 'm12-78'` (`sw.js:21`).
- **Brama:** `npm run brama` → **680 pass / 0 fail**, 0 naruszeń WCAG AA,
  oba szablony promptu zgodne z PROTOKOL. Na `main` było 692 — różnica to
  usunięte testy fallbacku Nominatim (−3) i rankingów (−14) oraz testy nowe (+5).
- Commity tej sesji: `6d8773f` plan, `b03ce81` audyt PR #12, `5aadacb` patch
  wgrany przez właściciela, `77dfe93` salvage (m12-75), `5a905f1`+`b8193a0`
  trasa-sekret (m12-76), `772429e` rankingi usunięte (m12-77), `536986f`
  `gra-opusc` + prywatność (m12-78), plus commit dokumentów.

## Co się zmieniło względem `main`

1. **Salvage sesji #12** (patch `01a09256-…`): poprawki właściciela 1, 2,
   3a–3e, 4a–4i — bez wyboru języka i podkładu, bez fallbacku Nominatim,
   setup multi z jednym graczem, trasa-sekret, lobby po wklejeniu paczki,
   wpisy „Host: <imię>", kanał info, koniec gry z ręki hosta, premia 3/2/1.
2. **4j — rankingi usunięte w całości**, także z mostu Drive. Gra kończy się
   podsumowaniem na telefonie; na Drive zostaje **historia gier**
   (`RO-gra/1`, stan `zakonczona`). Kody **R17/R18 wycofane**, numery zajęte
   na stałe. Klucze `okolica:ostatni-gracz` i `okolica:pseudonim` nie istnieją.
3. **Nowa akcja mostu `gra-opusc`** (PROTOKOL §9.5): wyjście z lobby prostuje
   skład gry, wyjście organizatora zamyka grę (stan `archiwum`). Po starcie
   odmawia — wyjście w trakcie gry to zdarzenie `rezygnacja`.
   `gra-hotseat` przesunął się na PROTOKOL **§9.6**.
4. **Trasa-sekret**: przycisk „🔄 Inny układ" jest `hidden` (nie tylko
   `disabled`), a komunikat rozróżnia stacje zlokalizowane na sieci dróg
   od układu pierścieniowego.

## Pułapki potwierdzone w tej sesji

- **Funkcje niesąsiadujące z usuwanym blokiem**: `przelaczSetup` siedział
  W ŚRODKU sekcji rankingów i wyleciał razem z nią — 16 testów UI padło na
  `ReferenceError`, nie na asercji. Przy wycinaniu sekcji grep po nazwach
  z `git diff | grep '^-function'`.
- **`git add` a cache-bust** (LESSONS L29): podbicie `?v=` dotyka WSZYSTKICH
  modułów `app/*.js`; commit z częścią grafu nie jest samodzielnie zielony
  (kontrakt pilnuje jednej wersji).
- Testy UI multi potrzebują `?tryb=test&odstep=0` (LESSONS L21) i atrapy mostu;
  ścieżka „sieć drogowa dostępna" idzie przez **cache** (`kluczCacheSieci`
  + `upraszczajDaneDoCache(parsujOdpowiedz(fixture))`), bo egress HTTPS
  w sandboxie jest ograniczony do `api.github.com` i `registry.npmjs.org`.

## Co zostało (właściciel zapowiedział: „poprawek będzie więcej")

- **Wdrożenie mostu**: `.gs` czeka na ponowne wklejenie do Apps Script
  (zmienił się: brak `rankingi()`, doszło `opuscGre()` + `case 'gra-opusc'`).
  Instrukcja: `docs/setup/most-drive-instrukcja.md`.
- **Test terenowy M11/M12** (WORKFLOW §4.4, teraz także krok 7a: wyjście
  z lobby na drugim telefonie).
- Z audytu PR #12 zostało: flaga `STAN.wymusPierscien` (`app/app.js:181`) jest
  czytana w trzech miejscach i **nigdzie nieustawiana** — albo dostaje
  przełącznik, albo wypada; braki
  dokumentacyjne (PROTOKOL §2.1/B21, README M4 + wysyłka + 🔌, WORKFLOW kroki
  3–5, ARCHITECTURE „przyciski degradacji", brak aneksów ADR 0020/0031,
  literówka „życią" w ADR 0017, nagłówek `test/duza-paczka.test.js`).
- ROADMAP: wszystkie 🟡 to „kod gotowy, czeka kryterium/właściciela"
  (M3–M8, M10–M12). Bez zlecenia bierz najwyższy niezamknięty kamień.
